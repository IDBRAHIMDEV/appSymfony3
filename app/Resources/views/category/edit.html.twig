{% extends 'back.html.twig' %}

{% block content %}
    <h3>Edition de Categorie</h3>

    {{ form_start(edit_form) }}
          <div class="form-group">
            {{ form_row(edit_form.libelle, {'attr': { 'class': 'form-control' }}) }}
        </div>

         <div class="form-group">
            {{ form_row(edit_form.slug, {'attr': { 'class': 'form-control' }}) }}
        </div>
        
         <div class="form-group">
            {{ form_row(edit_form.description, {'attr': { 'class': 'form-control' }}) }}
        </div>

        <hr>
        <input class="btn btn-danger" type="submit" value="Editer" />
          <a class="btn btn-default" href="{{ path('admin_category_index') }}">Retour</a>
    {{ form_end(edit_form) }}

    
{% endblock %}

{% block breadcrumb %}
  <ul class="breadcrumb">
      <li> <a href="">Tableau de bord</a> </li>
      <li> <a href="">Liste des catégories</a> </li>
      <li>edition de catégorie</li>
  </ul>
{% endblock %}

